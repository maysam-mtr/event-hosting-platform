/**
 * User Roles Enumeration
 * Defines the different user roles supported by the authentication system
 */

export enum Roles {
  ADMIN = "admin", // Full system access
  HOST = "host", // Event/session management access
  USER = "user", // Basic user access
}
